<!--pages/mine/course/mineCourse.wxml-->
<view class="tabCon">
  <button class="default_btn" data-tab="left" bindtap="tabChange">
    <view class="{{tab == 'left' ? 'current': ''}}" >已发布</view>
  </button>
  <button class="default_btn" data-tab="right" bindtap="tabChange">
    <view class="{{tab == 'right' ? 'current': ''}}">未发布</view>
  </button>
</view>

<view class="courseList" wx:if="{{tab=='left'}}">
  <view class="list" wx:for="{{courseList}}" wx:key="tiem.course_design_id">
    <navigator url="/pages/course/detail/detail">
      <view class="title">微机基础实验</view>
      <view class="desc">
      当点击  表单中 form-type 为 submit 的 组件时，会将表单组件中的 value 值进行提交，需要在表单组件中加上 name 来作为 key....
      </view>
      <view class="info">
        <view class="right">
          <text class="name">2019.2.22</text>
          <view class="star">
            <image src="../../../images/courseListStar.png"/>
            <text class="num">233</text>
          </view>
        </view>
      </view>
    </navigator>
  </view>
</view>
<view class="courseList" wx:else>
  <view class="list" wx:for="{{courseList}}" wx:key="tiem.course_design_id">
    <navigator url="/pages/course/detail/detail">
      <view class="title">微机基础实验</view>
      <view class="desc">
      a表单中 form-type 为 submit 的 组件时，会将表单组件中的 value 值进行提交，需要在表单组件中加上 name 来作为 key....
      </view>
      <view class="info">
        <view class="right">
          <text class="name">2019.2.22</text>
        </view>
      </view>
    </navigator>
  </view>
</view>
